/* @flow */
import asyncCatalog from './Catalog';
import asyncWorkOrder from './WorkOrder';
import asyncHome from './Home';
import asyncUserInfo from './UserInfo';
import NotFound from './NotFound';

export { asyncCatalog, asyncWorkOrder, asyncHome, asyncUserInfo, NotFound };
